const divisao = (dividendo, divisor) => {
  console.log(
    `${dividendo} / ${divisor} = ${dividendo / divisor}, resto = ${
      dividendo % divisor
    }`
  );
};

divisao(8, 4);
divisao(9, 2);
divisao(11, 4);
